export const processRow = (row) => {
  // TODO: Transform the row (a single row from your query)
  const processedRow = row;

  // Return the processed row (or null if you want to filter out the row)
  return processedRow;
};
